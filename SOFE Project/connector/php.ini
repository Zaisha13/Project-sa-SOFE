; Basic PHP configuration for SQLite
extension_dir = "ext"

; Enable SQLite extensions
extension=pdo_sqlite
extension=sqlite3

; Error reporting
display_errors = On
error_reporting = E_ALL

; Memory and execution limits
memory_limit = 128M
max_execution_time = 30